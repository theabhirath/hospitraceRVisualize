% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_visualization.R
\name{plot_trace_phylo_legend}
\alias{plot_trace_phylo_legend}
\title{Create standalone legend for trace phylogeny plots}
\usage{
plot_trace_phylo_legend(
  trace_colors = paletteer_d("ggthemes::Classic_Cyclic"),
  trace_labels = NULL,
  surv_colors = c(neg = "blue", pos_nonclust = "red", pos_clust = "gold"),
  label_colors = c(index = "red", `multiply-colonized-index` = "darkred", `weak-index` =
    "orange", convert = "black", `adm-pos` = "forestgreen", `adm-pos-convert` = "blue",
    `secondary-convert` = "grey", other = "purple"),
  include_trace = TRUE,
  include_surv = TRUE,
  include_patient = TRUE,
  title_size = 10,
  text_size = 8,
  key_size = 0.5,
  layout = c("horizontal", "vertical")
)
}
\arguments{
\item{trace_colors}{Color palette for trace location values. Should match
the colors used in \code{plot_trace_phylo_tree()}.}

\item{trace_labels}{Character vector of labels for each trace location.
Length should match \code{trace_colors}.}

\item{surv_colors}{Named character vector of colors for surveillance types.
Default matches \code{plot_trace_phylo_tree()} defaults.}

\item{label_colors}{Named character vector of colors for patient category labels.
Default matches \code{plot_trace_phylo_tree()} defaults.}

\item{include_trace}{Logical. Include trace location legend. Default TRUE.}

\item{include_surv}{Logical. Include surveillance legend. Default TRUE.}

\item{include_patient}{Logical. Include patient category legend. Default TRUE.}

\item{title_size}{Numeric. Font size for legend titles. Default 10.}

\item{text_size}{Numeric. Font size for legend text. Default 8.}

\item{key_size}{Numeric. Size of legend keys in cm. Default 0.5.}

\item{layout}{Character. Layout direction for combining legends.
\code{"horizontal"} places legends side-by-side (default),
\code{"vertical"} stacks them one below the other.}
}
\value{
A ggplot object containing only the legend(s), suitable for saving
with \code{ggsave()}.
}
\description{
Generates a combined legend plot for trace locations, surveillance results,
and patient categories that can be saved separately from the main plot.
}
