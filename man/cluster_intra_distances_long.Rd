% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_genetic_context.R
\name{cluster_intra_distances_long}
\alias{cluster_intra_distances_long}
\title{Compute the per-isolate genetic distance needed to cluster each isolate}
\usage{
cluster_intra_distances_long(
  clusters,
  snp_dist,
  linkage = c("single", "complete")
)
}
\arguments{
\item{clusters}{Vector named by sequence IDs giving each sequence's cluster.}

\item{snp_dist}{Matrix of SNP distances between isolates.}

\item{linkage}{Linkage rule, \code{"single"} or \code{"complete"}.}
}
\value{
A data frame with a \code{cluster} factor column and a numeric \code{distance} column.
}
\description{
Drops singletons and returns, per remaining cluster, one row per isolate giving the genetic
distance an SNV-threshold clustering would need to place that isolate in the cluster: its
distances to the other members aggregated by the linkage rule (nearest member for single,
farthest for complete). Clusters are visited in \code{sort(unique(...))} order so factor levels
come out sorted.
}
\keyword{internal}
