% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trace_phylo_tree.R
\name{plot_trace_phylo_tree}
\alias{plot_trace_phylo_tree}
\title{Plot a phylogeny with a location trace heatmap}
\usage{
plot_trace_phylo_tree(
  tree,
  isolate_lookup,
  trace_data,
  surv_df,
  cluster_filter = NULL,
  trace_colors = trace_color_palette(),
  surv_colors = surv_dot_colors(),
  surv_halos = surv_dot_halos(),
  surv_shapes = surv_dot_shapes(),
  clust_patient_categories = NULL,
  label_colors = c(index = "red", `multiply-colonized-index` = "darkred", `weak-index` =
    "orange", convert = "black", `adm-pos` = "forestgreen", `adm-pos-convert` = "blue",
    `secondary-convert` = "gray", `ambiguous-adm-pos` = "turquoise3", `ambiguous-convert`
    = "deeppink3", other = "purple"),
  inches_per_row = 0.2,
  row_thickness = 0.55,
  max_tree_width = 0.08,
  show_legend = TRUE
)
}
\arguments{
\item{tree}{A phylogenetic tree of class \code{phylo}.}

\item{isolate_lookup}{Data frame from \code{get_isolate_lookup()} with columns
isolate_id, patient_id, date, cluster, adm_pos, prev_surv.}

\item{trace_data}{Matrix with patient IDs as row names and numeric dates as column
names; values are location categories (0 = absent, 1+ = locations).}

\item{surv_df}{Surveillance data frame with columns patient_id, genome_id,
surv_date, result (0/1 for negative/positive).}

\item{cluster_filter}{Cluster IDs to include, or \code{NULL} for all.}

\item{trace_colors}{Color palette for trace location values.}

\item{surv_colors}{Named fill colors for surveillance types (neg, pos_clust,
pos_nonclust).}

\item{surv_halos}{Named outline (halo) colors, keyed like \code{surv_colors}.}

\item{surv_shapes}{Named integer plotting shapes (filled, 21-25), keyed like
\code{surv_colors}.}

\item{clust_patient_categories}{Named list from \code{cluster_patient_categorization()}
mapping patient_id to category per cluster; \code{NULL} labels all patients "other".}

\item{label_colors}{Named colors for patient category labels; "other" is the
fallback for any tip with no assigned category.}

\item{inches_per_row}{Height in inches per patient row when saving.}

\item{row_thickness}{Proportion of row spacing used for trace bar height.}

\item{max_tree_width}{Maximum tree width as a proportion of the time range; branch
lengths are scaled to fit.}

\item{show_legend}{Logical. Attach the figure's own legends (location, patient
status, surveillance); \code{FALSE} selects the separate-legend path.}
}
\value{
A ggplot object with the following attributes for consistent sizing:
\itemize{
\item \code{recommended_height}: Suggested height in inches
\item \code{recommended_width}: Suggested width in inches
\item \code{n_patients}: Number of patients in the plot
\item \code{inches_per_row}: Height allocated per patient row
}
Use these when saving with \code{ggsave()} to maintain consistent row heights.
When \code{show_legend = TRUE} the figure carries its own native
legends and the returned object is a single ggplot/ggtree;
\code{recommended_width} is widened to fit the legend.
}
\description{
Creates a combined visualization of a phylogenetic tree with a patient location
trace heatmap. Uses ggtree for the phylogeny with continuous line segments for
the trace data (not discrete cells). Surveillance data is overlaid as dots.
}
